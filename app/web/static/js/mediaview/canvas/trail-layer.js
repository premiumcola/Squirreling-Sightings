// ─── mediaview/canvas/trail-layer.js ───────────────────────────────────────
// Per-track motion trails (last N samples drawn as a polyline behind
// the current bbox). Toggleable via the canvas layer-toggle strip.
//
// SKELETON — task #4 fills this in.

export function renderTrailLayer(/* canvas, tracks, t */){}
