// ─── mediaview/panel-tabs.js ───────────────────────────────────────────────
// Dark panel-tab strip on near-black surface (#0a0e1a). Tabs sit on
// it as light pills; the active tab content block continues the same
// dark surface so the strip + active panel read as one zone.
//
// SKELETON — task #6 fills this in with the active/inactive tab
// styling, content-area surface, and tab switching.

export function renderPanelTabs(/* host, config */){}
