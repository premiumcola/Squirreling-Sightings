// ─── mediaview/panels/tracks-list.js ───────────────────────────────────────
// Track-Liste tab: one row per track (#1, #2, …) with class colour,
// span (start → end), end-reason, and last-score. Tap a row to seek
// the video to that track's start time.
//
// SKELETON — task #3 fills this in.

export function renderTracksListPanel(/* host, item */) {}
