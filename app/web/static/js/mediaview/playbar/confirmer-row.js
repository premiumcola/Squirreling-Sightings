// ─── mediaview/playbar/confirmer-row.js ────────────────────────────────────
// Structurally a swimlane with no class label: generic dots icon +
// "Confirmer" + 12-cell strip (gold = confirmed-window cell, grey =
// miss). Sits LAST so the playhead line cuts through it.
//
// SKELETON — task #4 fills this in.

export function renderConfirmerRow(/* host, confirmerCells */){}
