// ─── mediaview/panels/detections.js ────────────────────────────────────────
// Default-active tab in recorded mode: a chip strip listing every
// trigger detection (class · score · timestamp) plus the hidden-class
// chip strip from bbox-overlay/hidden-classes.js.
//
// SKELETON — task #3 fills this in. The legacy lightbox currently
// renders this same content under the video; task #3 relocates it
// into a panel tab.

export function renderDetectionsPanel(/* host, item */){}
