// ─── mediaview/panels/weather.js ───────────────────────────────────────────
// "Wetter" tab — only mounted when item carries weather data. Pulls
// from item.weather + weather-types.js for icon mapping. Read-only
// snapshot of conditions at recording time.
//
// SKELETON — task #3 fills this in.

export function renderWeatherPanel(/* host, item */) {}
