// ─── mediaview/playbar/scrubber.js ─────────────────────────────────────────
// Scrubber bar + draggable dot. Dot's left% comes from the shared
// --play-pct CSS variable so the scrubber, time-axis, and playhead
// line all sit at the same x without independent state.
//
// SKELETON — task #4 fills this in (lifts the relevant bits out of
// bbox-overlay/time-axis.js).

export function renderScrubber(/* host */){}
